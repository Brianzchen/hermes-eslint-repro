// @flow
export const home = '/';

export const recipesList = '/recipes';

export const recipe = `${recipesList}/:id`;

export const shoppingList = '/shopping-list';

export const households = '/households';

export const household = `${households}/:id`;
